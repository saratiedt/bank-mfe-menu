import { PdpjLinkDirective } from './pdpj-link.directive';

describe('PdpjLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new PdpjLinkDirective();
    expect(directive).toBeTruthy();
  });
});
